# 跳步噪声预测器初步评估

## 方法

旧版本在 FPGA 返回 `SKIP` 时直接复用最后一次真实 UNet 输出。新版本保存最近两次真实 UNet 输出，并根据 scheduler timestep 间距进行阻尼线性外推：

```text
predicted = latest + factor * (latest - previous)
factor = clamp(raw_timestep_ratio * 0.5, 0, 1.5)
```

如果真实输出不足两次，或者使用 `--skip-predictor reuse`，则自动回到旧值复用。

## 真实 PYNQ-Z2 验证

测试环境为 RTX 4060 Laptop GPU、PYNQ-Z2、SD 1.5、100 个基础时间步、动态阈值、距离阈值 `2.0`。三组实验都由 `run_pynq_speedup.cmd` 完整运行，包含 Baseline、Dynamic、质量指标、分项耗时和离线 HTML 报告。

| Seed | UNet / 跳步 | Baseline | Dynamic | 加速比 | PSNR | SSIM | LPIPS | CLIP 变化 |
| ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| 2167653148 | 50 / 50 | 10.87 s | 5.81 s | 1.87x | 35.407 dB | 0.9828 | 0.0079 | +0.195 |
| 3609869381 | 48 / 52 | 10.85 s | 5.58 s | 1.94x | 35.812 dB | 0.9774 | 0.0130 | +0.058 |
| 4290341620 | 51 / 49 | 9.94 s | 5.31 s | 1.87x | 25.751 dB | 0.9040 | 0.0434 | -0.650 |

三组均成功生成可识别的完整马匹，PYNQ 健康检查第一次通过，没有使用断线恢复。中位数加速比为 `1.87x`，中位数 PSNR 为 `35.407 dB`，中位数 SSIM 为 `0.9774`，中位数 LPIPS 为 `0.0130`。

## 结论与限制

阻尼线性预测已通过真实硬件流程验证，并且前两组非常接近 Baseline。第三组仍存在可见的腿部和草地细节差异，说明算法继续受随机种子影响，不能声称线性预测对所有图片都优于直接复用。

因此项目保留两种模式：

- 默认 `linear`：使用双历史阻尼线性预测。
- `--skip-predictor reuse`：恢复旧版本直接复用，用于回归和严格 A/B 实验。

在形成最终论文结论前，还需要对相同 seed 执行 `linear/reuse` 成对实验，并扩展到人物、建筑、车辆和复杂场景提示词。本文件只记录本次功能验证，不替代大样本统计结论。

