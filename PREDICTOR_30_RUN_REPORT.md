# SD 1.5 + PYNQ-Z2 30组预测器实验报告

## 实验设计

本实验使用 15 个固定随机种子。每个 seed 生成一次 100 步 Baseline，并分别运行：

- `linear`：最近两次真实 UNet 输出的阻尼线性外推。
- `reuse`：直接复用最后一次真实 UNet 输出。

因此共有 30 组 Dynamic 实验和 15 组严格同 seed A/B 对照。两个模式共享完全相同的 Baseline；15 对 Baseline 的 SHA-256 全部一致。模式运行顺序按 seed 交替，减少 GPU 温度和先后顺序偏差。

测试配置为 SD 1.5、100 个扩散时间步、PYNQ-Z2 动态阈值、int8 下采样特征、距离阈值 `2.0`、当前马匹提示词。

## 汇总结果

| 指标 | linear | reuse |
| --- | ---: | ---: |
| 实验组数 | 15 | 15 |
| UNet 执行中位数 | 50 | 51 |
| 跳步中位数 | 50 | 49 |
| 加速比平均值 | 1.852x | 1.857x |
| 加速比中位数 | 1.841x | 1.836x |
| PSNR 平均值 | 34.665 dB | 31.957 dB |
| PSNR 中位数 | 36.570 dB | 33.683 dB |
| SSIM 平均值 | 0.9595 | 0.9428 |
| SSIM 中位数 | 0.9828 | 0.9628 |
| LPIPS 平均值 | 0.0219 | 0.0329 |
| LPIPS 中位数 | 0.0079 | 0.0196 |
| CLIP 变化平均值 | +0.0964 | +0.1103 |

## 同 seed A/B 结论

`linear - reuse` 的成对结果：

| 指标差值 | 平均 | 中位数 | 范围 |
| --- | ---: | ---: | ---: |
| 加速比 | -0.0055x | -0.0020x | -0.0927 至 +0.0481 |
| PSNR | +2.708 dB | +2.699 dB | +0.272 至 +5.282 |
| SSIM | +0.0167 | +0.0160 | +0.0015 至 +0.0455 |
| LPIPS | -0.0110 | -0.0108 | -0.0274 至 -0.0023 |
| CLIP变化 | -0.0139 | -0.0233 | -0.3128 至 +0.4944 |
| 跳步数 | +0.067 | 0 | 0 至 +1 |

15/15 个 seed 的 PSNR、SSIM 和 LPIPS 都由 `linear` 改善。双侧符号检验 `p=0.00006104`，说明这三个全参考指标的改善不是随机正负波动。加速比和跳步数基本不变，因此质量改善没有明显牺牲速度。

CLIP变化只有 7/15 个 seed 由 `linear` 改善，平均差值接近 0。线性预测改善的是 Dynamic 对 Baseline 的一致性，不能证明文本对齐一定更好。

## 稳定性风险

| 风险项 | linear | reuse |
| --- | ---: | ---: |
| PSNR < 30 dB | 5/15 | 5/15 |
| SSIM < 0.90 | 2/15 | 3/15 |
| Dynamic CLIP低于Baseline | 6/15 | 6/15 |

`linear` 最差 PSNR 为 `23.807 dB`，最好为 `43.934 dB`。虽然它对所有 seed 都优于 `reuse`，但没有消除最差 seed，生成质量仍然明显依赖初始噪声。

## 耗时和数据流

30 组平均值：

| 部分 | 耗时 |
| --- | ---: |
| Dynamic 总生成 | 5260.1 ms |
| UNet | 4847.3 ms |
| PYNQ 总处理 | 173.7 ms |
| 网络往返 | 156.5 ms |
| FPGA kernel | 58.2 ms |
| 跳步预测 | 3.85 ms |

UNet 约占 Dynamic 总时间的 `92.2%`。每张图发送约 `400 KiB` 特征。PYNQ、网络和 FPGA 数值存在包含关系，不能作为独立阶段重复相加。

30 组共记录 3000 个时间步，其中 2970 步有参考向量。距离门通过 2970 步、拒绝 0 步；说明距离阈值 `2.0` 没有实际筛选作用，当前双重判断实际上退化为余弦相似度单门。

整个批次 PYNQ 通信成功，断线整轮恢复次数为 0。

## 当前不足

1. `linear`显著优于`reuse`，但仍有 5/15 组 PSNR 低于 30 dB，最差 seed 问题没有解决。
2. 距离阈值 `2.0` 让距离门全部通过，余弦和距离双重判断名义上存在，实际只有余弦在控制跳步。
3. 30组实验只覆盖马匹提示词，不能代表人物、建筑、车辆、风景和复杂多主体场景。
4. FPGA只负责特征统计和跳步判断，UNet仍在PC GPU；约1.85x加速主要来自跳过UNet，不能全部归因于FPGA计算。
5. PSNR、SSIM和LPIPS衡量 Dynamic 是否接近 Baseline，不衡量 Baseline 本身是否符合提示词。例如 seed `2499984135` 的两张图都出现了人物，偏离了 `single horse` 的意图。
6. CLIP只使用一个模型和一个提示词，不能代替人工盲评、目标检测、马匹解剖正确性或多模型语义评分。
7. 当前阻尼系数、最大外推系数和动态阈值对所有阶段固定，没有针对早期结构形成和后期细节恢复分别优化。
8. 尚缺少“PC端执行相同跳步算法”的对照，因此不能单独量化 FPGA 相对 CPU/GPU 判断的净收益。

## 文件

完整批次位于：

```text
experiments/predictor_30run_20260815_2005/
```

- `results.csv`：30组逐组中英双语结果。
- `batch_summary.json`：完整统计、成对差值和诊断数据。
- `batch_report.html`：可直接打开的简单HTML报告。
- 每个 `seed_<seed>_<mode>/report.html`：图片、逐步曲线、耗时和质量指标。

当前证据支持将 `linear` 作为优于直接复用的默认策略，但不足以声称当前控制器已经质量安全。

